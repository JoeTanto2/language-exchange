import React from 'react';
import Header from '../header';
import Footer from '../footer';

import './home.scss';

const Home = () => {
    return (
        <React.Fragment>
            <Header />
            <Footer />
        </React.Fragment>
    );
};

export default Home;
